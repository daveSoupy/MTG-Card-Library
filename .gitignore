# macOS
.DS_Store

# Node
node_modules/
*.log
npm-debug.log*

# Build output
dist/
build/
.vite/

# Local database and cached card images (rebuilt from Scryfall bulk data)
data/
*.sqlite
*.sqlite-wal
*.sqlite-shm
*.db
*.db-wal
*.db-shm

# TypeScript incremental build state
*.tsbuildinfo

# Internal design and reference docs — kept on disk, deliberately not published.
# Removed from the repo and its history on 2026-09-22; the public README no
# longer links to them. Claude Code still auto-loads CLAUDE.md from disk.
/phases/
/docs/
/CLAUDE.md
/desktop/CLAUDE.md
/server/CLAUDE.md
