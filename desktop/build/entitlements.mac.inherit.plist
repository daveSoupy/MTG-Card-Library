<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Hardened-runtime entitlements for every Mach-O in the bundle other than the
  .app: Electron's helpers and frameworks, and — the reason this file exists
  as its own copy — the bundled Node at Resources/node/node and the
  better_sqlite3.node it loads. Node JITs, so a hardened-runtime signature
  without allow-jit is a crash on the first search. Same three keys as
  entitlements.mac.plist; see that file for what each one is for.
-->
<plist version="1.0">
  <dict>
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
  </dict>
</plist>
